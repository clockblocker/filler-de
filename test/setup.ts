// Bun test setup file
// Add any global test setup here if needed

// Example mock setup for Bun (uncomment if needed):
// import { mock } from 'bun:test';

// mock.module('utils', () => {
// 	return {
// 		// Mirror the runtime formatting used in Obsidian for tests
// 		// We intentionally omit the trailing newline here because the caller joins with "\n"
// 		wrapTextInBacklinkBlock: ({
// 			text,
// 			fileName,
// 			linkId,
// 		}: {
// 			text: string;
// 			fileName: string;
// 			linkId: number;
// 		}) => ` ${text} ^${linkId}`,
// 	};
// });
